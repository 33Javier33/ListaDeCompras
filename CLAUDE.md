# ListaDeCompras — CLAUDE.md

## Reglas de trabajo en este repositorio

- **Push automático**: todo cambio se sube a `main` de inmediato al terminar, sin pedir confirmación, a menos que el usuario indique explícitamente "lo revisaré" o similar.
- **Rama de trabajo**: `main` (salvo instrucción contraria).
- **Stack**: HTML + CSS + JS vanilla, Tailwind CDN, SortableJS CDN, PWA (manifest + sw.js).
